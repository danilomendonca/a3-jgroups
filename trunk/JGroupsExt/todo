merge view (più avanti)

Generic:
		if(supervisor == null && exist(groupname)){
				if(generic.getSupervisorRole(groupName)!=null){
						wait until the end of the election
						if(generic.isElected)
								do nothing
						else
								if(generic.getFollowerRole(groupName)!=null){
									activate(followerRole(groupName));
									
SupervisorElection:
		last generic that joins the group doesn't participate in the election
		(use MessageOverMemory???)
		
		
8/5

limite numero tentativi per eleggere il supervisore		

return sendMessageToSupervisor -----> boolean/Eccezione

inserimento mex in memoria
	- teniamo sempre traccia dell'index
	- ripristiniamo l'index quando un nuovo supervisore viene online

cancellazione mex in memoria:
		-index (salvato come informazione applicativa - non nel nostro controllo)
		-automatico (ora/giorno di scadenza)